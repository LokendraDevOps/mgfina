import PlaceholderPage from '@/components/common/PlaceholderPage';

const NotFound = () => {
  return <PlaceholderPage title="Page Not Found" description="The route you requested does not exist in this foundation build." />;
};

export default NotFound;
