import { useMemo,useState } from 'react';import banks from '@/data/banks.json';import { PageHero,SearchBox,Section } from '@/components/platform';import Button from '@/components/ui/Button';
export default function Compare(){const [q,setQ]=useState(''),[sort,setSort]=useState('rate'),[maxRate,setMaxRate]=useState(20);const rows=useMemo(()=>banks.filter(x=>x.name.toLowerCase().includes(q.toLowerCase())&&x.rate<=maxRate).sort((a,b)=>sort==='rate'?a.rate-b.rate:b.rating-a.rating),[q,sort,maxRate]);return <><PageHero eyebrow="Compare loans" title="Better decisions begin side by side" description="Search, filter and compare indicative loan offers from leading financial institutions."/><Section><div className="mb-6 grid gap-4 rounded-2xl border border-slate-200 bg-white p-4 md:grid-cols-3"><SearchBox value={q} onChange={setQ} placeholder="Search a bank"/><label className="flex items-center gap-3 text-sm">Max rate <input type="range" min="8" max="25" value={maxRate} onChange={e=>setMaxRate(+e.target.value)} className="flex-1 accent-blue-600"/><b>{maxRate}%</b></label><select value={sort} onChange={e=>setSort(e.target.value)} className="rounded-xl border border-slate-200 px-4"><option value="rate">Lowest interest first</option><option value="rating">Highest rated first</option></select></div><div className="overflow-x-auto rounded-2xl border border-slate-200 bg-white"><table className="w-full min-w-[760px] text-left"><thead className="bg-slate-950 text-white"><tr>{['Bank','Interest from','Processing fee','Max tenure','Rating',''].map(x=><th key={x} className="p-4">{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.name} className="border-t border-slate-100"><td className="p-4 font-bold">{x.name}</td><td className="p-4 text-blue-600 font-bold">{x.rate}% p.a.</td><td className="p-4">{x.fee}</td><td className="p-4">{x.tenure}</td><td className="p-4">★ {x.rating}</td><td className="p-4"><Button as="a" href="/apply" className="py-2">Apply</Button></td></tr>)}</tbody></table>{!rows.length&&<p className="p-10 text-center text-slate-500">No offers match these filters.</p>}</div><p className="mt-4 text-xs text-slate-500">Rates and fees are indicative dummy data for demonstration purposes only.</p></Section></>}
