import PlaceholderPage from '@/components/common/PlaceholderPage';

const Login = () => {
  return (
    <PlaceholderPage
      title="Login"
      description="Authentication UI is intentionally left as a placeholder for the next phase."
    />
  );
};

export default Login;
